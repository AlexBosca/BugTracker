name: Frontend CI/CD

on:
  workflow_dispatch:  # This allows manual triggering of the workflow

jobs:
  frontend:
    name: Build and Test Frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Cache Node.js modules
        uses: actions/cache@v3
        with:
          path: frontend/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm install
        working-directory: frontend

      - name: Lint code
        run: npm run lint
        working-directory: frontend

      - name: Run Unit Tests
        run: npm test
        working-directory: frontend

      - name: SonarCloud Scan
        run: npx run sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.projectKey=bugtracker_frontend -Dsonar.token=${{ secrets.SONAR_TOKEN }}
        working-directory: frontend

# name: Frontend CI/CD

# on:
#   # repository_dispatch:
#   #   types: [trigger-frontend]
#   workflow_dispatch:

#   push:
#     paths:
#       - 'frontend/**'
#   pull_request:
#     paths:
#       - 'frontend/**'

# jobs:
#   frontend:
#     name: Build and Test Frontend
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'

#       - name: Cache Node.js modules
#         uses: actions/cache@v3
#         with:
#           path: ~/.npm
#           key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
#           restore-keys: |
#             ${{ runner.os }}-node-

#       - name: Install dependencies
#         uses: npm install

#       - name: Lint Code
#         uses: npm run Lint

#       - name: Run Unit Tests
#         uses: npm test

#       - name: SonarCloud Scan
#         run: npx run sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.projectKey=bugtracker_frontend -Dsonar.token=${{ secrets.SONAR_TOKEN }}
